#!/usr/bin/env python3

# author: Tobias Köhne

# get the package
import altar


# make a specialized app that uses this model by default
class SEAS(altar.shells.application, family='altar.applications.seas'):
    """
    A specialized AlTar application that exercises the SEAS model
    """

    # user configurable state
    model = altar.models.model(default='seas')
    model.doc = "the AlTar model to sample"


# bootstrap
if __name__ == "__main__":
    # build an instance of the default app
    app = SEAS(name="SEAS")
    # invoke the main entry point
    status = app.run()
    # share
    raise SystemExit(status)
